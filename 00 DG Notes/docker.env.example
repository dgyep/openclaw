# OpenClaw Docker Deployment - Environment Variables
# Copy this to ~/openclaw/.env on your VPS and fill in the values.
#
# Required variables (docker-compose.yml references these):

# --- Paths (required) ---
# Where openclaw.json and runtime config live on the host
OPENCLAW_CONFIG_DIR=/root/.openclaw
# Where agent workspaces are stored on the host
OPENCLAW_WORKSPACE_DIR=/root/.openclaw/workspace

# --- API Keys (at least one model provider required) ---
# OpenRouter API key (provides access to multiple models)
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxx

# --- Optional: Claude session keys (for Claude web provider) ---
CLAUDE_AI_SESSION_KEY=
CLAUDE_WEB_SESSION_KEY=
CLAUDE_WEB_COOKIE=

# --- Optional: Gateway settings ---
# Docker image name (default: openclaw:local, built locally)
# OPENCLAW_IMAGE=openclaw:local
# Gateway bind mode: lan (default), loopback, or public
# OPENCLAW_GATEWAY_BIND=lan
# Gateway port (default: 18789)
# OPENCLAW_GATEWAY_PORT=18789
# Bridge port (default: 18790)
# OPENCLAW_BRIDGE_PORT=18790
# Gateway access token (optional, secures WebSocket connections)
# OPENCLAW_GATEWAY_TOKEN=your-secret-token
